# Keep dependency resolution strict and reproducible across the workspace.
strict-peer-dependencies=false
# Internal @issue-to-pr/* packages are consumed as source (no build step); this
# makes sure their transitive deps are hoisted so tsx/vitest resolve them.
shamefully-hoist=false
