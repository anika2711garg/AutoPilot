# Dependencies (anywhere in the monorepo)
node_modules/
.pnpm-store/

# Build output
dist/
.next/
out/
build/
*.tsbuildinfo
next-env.d.ts

# Env / secrets — never commit (but keep the example)
.env
.env.*
!.env.example

# Test / coverage
coverage/

# OS / editor
.DS_Store
*.pem
*.log
